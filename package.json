{
  "name": "constrained-optimization",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel public/index.html --port 8000 --open",
    "dev": "npm start",
    "build": "rm -rf dist && parcel build public/index.html --target browser --out-dir dist --public-url ./",
    "deploy": "npm run build && firebase deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "styled-components": "^5.2.0"
  },
  "devDependencies": {
    "@babel/core": "^7.11.6",
    "babel-plugin-styled-components": "^1.11.1",
    "firebase-tools": "^8.11.2",
    "parcel-bundler": "^1.12.4"
  },
  "babel": {
    "plugins": [
      [
        "babel-plugin-styled-components",
        {
          "ssr": false
        }
      ]
    ]
  }
}
